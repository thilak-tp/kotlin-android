package com.forktail.myfirstproject
// Main is the entry point and is a function which take command line arguments and stores it as an array of strings
fun main(args:Array<String>)
{
    // This prints hello world on the screen, print doesn't implicitly add a newline character at
    // println does
    print("Hello World!")
}